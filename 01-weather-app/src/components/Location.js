import React from 'react';

const Location = ( { city } ) => (
// const Location = ( props ) => {
    // const city = props.city;
    // const { city } = props;
    <div>
        <h1>
            { city }
        </h1>
    </div>
);

export default Location;